@model MvcCrud.Models.Dto.CategoriesModel

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Sil</title>
</head>
<body>
    <h3>Silmek istediğinize emin misiniz?</h3>
    <div>
        <h4>CategoriesModel</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>@Html.DisplayNameFor(model => model.categories.CategoryName)</dt>
            <dd>@Html.DisplayFor(model => model.categories.CategoryName)</dd>
            <dt>@Html.DisplayNameFor(model => model.categories.Description)</dt>
            <dd>@Html.DisplayFor(model => model.categories.Description)</dd>
        </dl>
    
        @using (Html.BeginForm()) {
            @Html.AntiForgeryToken()
    
            <div class="form-actions no-color">
                <input type="submit" value="Sil" class="btn btn-danger" /> |
                @Html.ActionLink("Listeye Geri Dön", "Index")
            </div>
        }
    </div>
</body>
</html>
